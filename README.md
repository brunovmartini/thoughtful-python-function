# Thoughtful Python Function

Thoughtful Python Function is a Python function for sorting packages based on its width, height, length and mass.

### Requirements 📋

- Python 3+ installed.
- Pytest installed.

---

### Running the function ⚙️

To run the function simply update the values of the last line of the file `thoughtful_sort.py` and type `python thoughtful_sort.py` on the `thoughtful` folder of the repository.

---

### Tests ✅

To run the tests simply run the command `pytest` on the root folder of the repository.
