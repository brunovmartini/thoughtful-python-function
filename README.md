# Thoughtful Python Function

Thoughtful Python Function is a Python function for sorting packages based on its width, height, length and mass.

### Requirements 📋

- Python 3+ installed.

---

### Running the function ⚙️

To run the function simply update the values of the last line of the file `function.py` and type `python function.py` on the root folder of the repository,
